<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_300</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/drawer_icon</item>
    </style>

    <style name="AppThemeWithWelcome" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="colorPrimaryDark">@color/black</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DrawerArrowWhite" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/white</item>
    </style>

    <style name="SwitchFemaleOnly" parent="Widget.AppCompat.CompoundButton.Switch">
        <!-- Active thumb color & Active track color(30% transparency) -->
        <item name="colorControlActivated">@color/female_only_pink</item>
        <!-- Inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/grey_300</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/grey_600</item>
        <item name="android:colorControlActivated">@color/female_only_pink</item>
    </style>

    <style name="SwitchAccent" parent="Widget.AppCompat.CompoundButton.Switch">
        <!-- Active thumb color & Active track color(30% transparency) -->
        <item name="colorControlActivated">@color/tint_color</item>
        <!-- Inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/grey_300</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/grey_600</item>
        <item name="android:colorControlActivated">@color/tint_color</item>
    </style>

</resources>
